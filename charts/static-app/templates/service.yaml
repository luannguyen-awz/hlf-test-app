apiVersion: v1
kind: Service
metadata:
  name: {{ include "static-app.fullname" . }}
  labels:
    {{- include "static-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "static-app.selectorLabels" . | nindent 4 }}
